# Arrays
```java
  Arrays.copyOf(arr, arr.length)
  Arrays.copyOfRange(arr, 2, 4)
  Arrays.fill(arr, 9)
  Arrays.setAll(arr, () -> (int)(Math.random() * 5)+1);
  Arrays.sort()
  Arrays.binarySearch()
  Arrays.toString()
  Arrays.deepToString()
  Arrays.equals()
  Arrays.deepEquals()
  Arrays.asList() #불편
  Arrays.parallelXXX()
  Arrays.spliterator() #여러 쓰레트가 처리할 수 있게 하나의 작업을 여러 작업으로 나누는 Spliterator를 반환
  Arrays.stream()
```
